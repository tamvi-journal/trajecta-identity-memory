# Keep LF everywhere so hashes and SQL are byte-identical on every OS.
* text=auto eol=lf
# Canonical document: its sha256 is pinned in memory_core.vho. Never convert.
docs/VECTOR-HUMAN-ONTOLOGY.md -text
